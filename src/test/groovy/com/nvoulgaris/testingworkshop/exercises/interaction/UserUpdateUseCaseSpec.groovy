package com.nvoulgaris.testingworkshop.exercises.interaction

import spock.lang.Specification

class UserUpdateUseCaseSpec extends Specification {

    def "should throw a RuntimeException when user update event is null"() {
    }

    def "should not update the user when cannot fetch the user"() {
    }

    def "should not update the user when cannot be updated by the event"() {
    }

    def "should update a user based on the received user update event"() {
    }

    def "should return the updated and saved user"() {
    }
}
